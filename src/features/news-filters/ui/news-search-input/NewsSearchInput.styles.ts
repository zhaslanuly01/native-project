import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 42,
    borderWidth: 1,
    borderColor: "#e5e5e5",
    borderRadius: 10,
    paddingHorizontal: 12,
    backgroundColor: "#fff",
    color: "#111",
  },
});
